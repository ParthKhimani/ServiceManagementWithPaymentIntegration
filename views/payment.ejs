<!DOCTYPE html>
<html>
<title>Stripe Payment Demo</title>

<body>
    <br>
    <br>
    <center>
        <h3>Welcome <%= customerName %>, Click below to pay</h3>
        <form action="/paymentCompleted" method="POST">
            <input type="hidden" value="<%= customerName %>" name="customer">
            <input type="hidden" value="<%= amountCharged %>" name="amount">
           
            <script src="//checkout.stripe.com/v2/checkout.js" class="stripe-button" data-key="<%= key %>"
                data-amount="<%= amountCharged * 100 %>*" data-currency="inr" data-name="<%= customerName %>"
                data-description="provide your card details" data-locale="auto">
                </script>
        </form>
    </center>
</body>

</html>